 
<h1><%= @listing.property_name %></h1>

 <tr>
      <td>Description: <%= @listing.property_description %></td></br>
      <td>Capacity: <%= @listing.max_guest_number %> pax</td></br></br>
      <td>Price Per Night: $<%= @listing.price %></td></br></br>
      <td><%= @listing.country %></td></br>
      <td><%= @listing.city %></td></br></br>
 </tr>
 


 <!-- Uploaded image display. -->
  <% @listing.images.each do |image| %>
      <%= image_tag image.url(:medium) %>
  <% end %>
</br>
<%= link_to ("Edit Listing"), edit_listing_path %>
<!-- By default links are GET methods! You must declare. -->
<%= link_to ("Delete Listing"), destroy_listing_path, method: :delete %>

<%= link_to ("Make a Reservation"), new_listing_reservation_path(@listing.id) %></br></br>


 <!-- AREA FOR RESERVATION DETAILS / NEED TO BUILD AUTHORIZATION -->

<h3>Current Bookings:</h3>

  
  <% @listing.reservations.each do |reservation| %>
  
    <tr>
      <td>Guest Name: <%= reservation.user.first_name %></td></br>
      <td>Start Date: <%= reservation.start_date %></td></br>
      <td>End Date: <%= reservation.start_date %></td></br>

  <% end %>

<p>______________________</p>
</br></br></br>

 <!-- BOOTSTRAP CAROUSEL / IN PROGRESS -->


<div id="carouselExampleControls" class="carousel slide" data-ride="carousel">
  <div class="carousel-inner">
    <div class="carousel-item active">
      <img class="d-block w-100" src="<% @listing.images[0] %>" alt="First slide">
    </div>
    <div class="carousel-item">
      <img class="d-block w-100" src="..." alt="Second slide">
    </div>
    <div class="carousel-item">
      <img class="d-block w-100" src="..." alt="Third slide">
    </div>
  </div>
  <a class="carousel-control-prev" href="#carouselExampleControls" role="button" data-slide="prev">
    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
    <span class="sr-only">Previous</span>
  </a>
  <a class="carousel-control-next" href="#carouselExampleControls" role="button" data-slide="next">
    <span class="carousel-control-next-icon" aria-hidden="true"></span>
    <span class="sr-only">Next</span>
  </a>
</div>